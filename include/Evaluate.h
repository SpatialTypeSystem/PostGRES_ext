#ifndef EVALUATE_H
#define EVALUATE_H

enum class PREDICATE { disjoint, meet, overlap, covers, contains, equal, inside, coveredBy };

class Evaluate {
public:
    Evaluate();
    ~Evaluate();
private:
};

#endif //EVALUATE_H
